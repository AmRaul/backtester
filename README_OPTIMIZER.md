# 🚀 Strategy Optimizer - Quick Start

Ваш Telegram ID **297936848** уже вшит в код как админ оптимизатора!

---

## ⚡ Самый быстрый способ (1 команда)

```bash
./RUN_OPTIMIZATION.sh
```

Это запустит оптимизацию БЕЗ индикаторов (чистая DCA стратегия) на 200 trials.

---

## 🎯 Варианты запуска

### С индикаторами (RSI + EMA)
```bash
./RUN_OPTIMIZATION.sh --with-indicators
```

### Без индикаторов (чистая DCA)
```bash
./RUN_OPTIMIZATION.sh --no-indicators
```

### Кастомное количество trials
```bash
./RUN_OPTIMIZATION.sh --trials 150
```

### Свой конфиг
```bash
./RUN_OPTIMIZATION.sh --config my_optimization.json --trials 100
```

---

## 🌐 Через Web UI

1. Откройте: http://localhost:8000/optimize
2. Ваш ID **297936848** уже подставлен
3. Настройте параметры
4. Нажмите "Start Optimization"

---

## 📱 Уведомления Telegram

Вы будете получать:
- **Старт**: "🚀 Запуск оптимизации..."
- **Прогресс**: Каждые 20% (40/100)
- **Завершение**: Полные результаты + лучшие параметры

---

## 📊 Конфигурации

### optimization_config_with_indicators.json
- ✅ Оптимизирует RSI, EMA параметры
- ✅ 150 trials рекомендуется
- ✅ ~18 минут
- ⚠️ Больше риск overfitting

### optimization_config_no_indicators.json
- ✅ Только DCA + TP/SL
- ✅ 200 trials рекомендуется
- ✅ ~25 минут
- ✅ Проще, надёжнее

### optimization_config_example.json
- Базовый пример для создания своих конфигов

---

## 🔍 Мониторинг

### Статус очереди
```bash
curl http://localhost:8000/api/optimize/queue
```

### Результаты
```bash
# Получить task_id из вывода команды или Telegram
curl http://localhost:8000/api/optimize/results/TASK_ID
```

### Логи
```bash
docker logs -f backtester_web
```

---

## 💾 Сохранение результатов

Лучшая конфигурация автоматически:
- 💾 Сохраняется в БД (`optimization_results`)
- 💾 Экспортируется в JSON (`results/optimization_*.json`)

Для использования в бэктестах:
```bash
curl -X POST http://localhost:8000/api/optimize/save-config/TASK_ID \
  -H "X-User-ID: 297936848"
```

---

## 🛠️ Добавить других админов

См. **HOW_TO_ADD_OPTIMIZER_ADMIN.md**

---

## 📚 Полная документация

- **Quick Start**: `OPTIMIZATION_QUICKSTART.md`
- **Docker Guide**: `DOCKER_OPTIMIZATION_GUIDE.md`
- **Admin Management**: `HOW_TO_ADD_OPTIMIZER_ADMIN.md`

---

**Готово! Запускайте оптимизацию прямо сейчас! 🎉**
